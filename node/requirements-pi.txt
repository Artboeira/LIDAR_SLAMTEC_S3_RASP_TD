# Deps mínimas do node/ (§5.1 item 4 do guia).
# ABSOLUTAMENTE nada de pygame/tkinter/customtkinter aqui — o Pi é headless.

rplidar-roboticia
pyserial          # importado direto por lidar_reader.py (serial cru + list_ports)
numpy
pyyaml
ruamel.yaml       # não usado por node/ em runtime; fica para as ferramentas de bancada
